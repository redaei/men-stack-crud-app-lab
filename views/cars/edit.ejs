<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= car.make %> <%= car.model %></title>
  </head>
  <body>
    <h1>Edit <%= car.make %> <%= car.model %></h1>

    <form action="/cars/<%=car._id%>?_method=PUT" method="POST">
      <label for="make">Make:</label>
      <input type="text" name="make" id="make" value="<%= car.make %>" /> <br />

      <label for="model">Model:</label>
      <input type="number" name="model" id="model" value="<%= car.model %>" />
      <br />

      <label for="fuelType">Fuel Type:</label>
      <select name="fuelType" id="fuelType" value="<%= car.fuelType %>">
        <option value="Gasoline">Gasoline</option>
        <option value="Diesel">Diesel</option>
      </select>

      <br />

      <label for="numberOfDoors">Number Of Doors:</label>
      <input
        type="number"
        name="numberOfDoors"
        id="numberOfDoors"
        value="<%= car.numberOfDoors %>"
      />
      <br />

      <label for="isUsed">Used?</label>
      <input
        type="checkbox"
        name="isUsed"
        id="isUsed"
        <% if(car.isUsed){ %> checked <% } %> />
      <br />

      <button type="submit">Update Car</button>
    </form>

    <a href="/cars">Back to Cars</a>
  </body>
</html>
